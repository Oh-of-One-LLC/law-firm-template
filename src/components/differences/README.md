# Differences Card

Basic differences graph that lists what you do and shows that the competition doesn't.

## Usage

Relied on an array of type `{ feature: string; ours: boolean; theirs: boolean }[]` in `./config.ts`.

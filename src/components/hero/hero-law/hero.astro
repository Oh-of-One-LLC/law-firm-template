<section class="relative min-h-screen overflow-hidden pt-12 bg-brand">
  <!-- Radial gradient overlays -->
  <div
    class="hero-overlay-gradient pointer-events-none absolute inset-0"
    aria-hidden="true"
  >
  </div>
  <!-- Subtle grid texture -->
  <div
    class="hero-overlay-grid pointer-events-none absolute inset-0"
    aria-hidden="true"
  >
  </div>

  <div
    class="relative z-10 mx-auto grid max-w-6xl grid-cols-1 gap-12 px-8 py-20 md:grid-cols-2 md:items-center md:py-28 lg:px-12"
  >
    <!-- Left: text content -->
    <div class="hero-content flex flex-col gap-6">
      <p
        class="hero-eyebrow flex items-center gap-3 text-xs font-medium tracking-widest uppercase text-accent"
      >
        <span class="inline-block h-px w-8 bg-accent"></span>
        Business Law &middot; Denver, Colorado
      </p>

      <h1
        class="hero-heading font-display text-4xl font-bold leading-tight tracking-tight text-white md:text-5xl lg:text-6xl"
      >
        Counsel built for <em class="italic text-accent">business,</em>
        not bureaucracy.
      </h1>

      <p
        class="hero-desc max-w-lg text-base leading-relaxed font-light text-brand-fg/70 md:text-lg"
      >
        Mercer &amp; Aldine advises Denver businesses through their most
        critical legal decisions — from formation and contracts to M&amp;A and
        dispute resolution. Straightforward counsel. No billable-hour surprises.
      </p>

      <div class="hero-actions flex flex-wrap items-center gap-4 pt-2">
        <a
          href="/#call"
          class="rounded-sm bg-accent px-8 py-4 text-sm font-medium tracking-[0.06em] uppercase text-white transition hover:opacity-90 focus:ring-2 focus:ring-white focus:ring-offset-2 focus:outline-none"
        >
          Schedule a Consultation
        </a>
        <a
          href="/#services"
          class="flex items-center gap-2 text-sm font-light uppercase tracking-[0.06em] text-brand-fg/70 transition hover:text-brand-fg focus:ring-2 focus:ring-white focus:outline-none"
        >
          Our Practice Areas <span aria-hidden="true">&rarr;</span>
        </a>
      </div>
    </div>

    <!-- Right: stat card -->
    <div class="hero-card hidden justify-center md:flex">
      <div
        class="w-full max-w-sm rounded-sm border border-brand-fg/12 bg-brand-fg/6 p-12 backdrop-blur"
      >
        <div class="mb-8 border-b border-brand-fg/10 pb-8">
          <p class="mb-2 font-display text-[2.75rem] font-semibold leading-none text-accent">
            18+
          </p>
          <p class="text-sm font-light tracking-[0.04em] text-brand-fg/60">
            Years serving Denver businesses
          </p>
        </div>
        <div class="mb-8 border-b border-brand-fg/10 pb-8">
          <p class="mb-2 font-display text-[2.75rem] font-semibold leading-none text-accent">
            400+
          </p>
          <p class="text-sm font-light tracking-[0.04em] text-brand-fg/60">
            Transactions closed
          </p>
        </div>
        <div>
          <p class="mb-2 font-display text-[2.75rem] font-semibold leading-none text-accent">
            96%
          </p>
          <p class="text-sm font-light tracking-[0.04em] text-brand-fg/60">
            Client retention rate
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-overlay-gradient {
    background-image: radial-gradient(
        ellipse at 80% 20%,
        rgba(90, 140, 106, 0.15) 0%,
        transparent 60%
      ),
      radial-gradient(
        ellipse at 20% 80%,
        rgba(184, 150, 62, 0.08) 0%,
        transparent 50%
      );
  }

  .hero-overlay-grid {
    background-image: linear-gradient(
        rgba(245, 240, 232, 0.03) 1px,
        transparent 1px
      ),
      linear-gradient(90deg, rgba(245, 240, 232, 0.03) 1px, transparent 1px);
    background-size: 60px 60px;
  }

  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(24px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .hero-eyebrow {
    animation: fadeUp 0.6s 0s ease both;
  }
  .hero-heading {
    animation: fadeUp 0.6s 0.1s ease both;
  }
  .hero-desc {
    animation: fadeUp 0.6s 0.2s ease both;
  }
  .hero-actions {
    animation: fadeUp 0.6s 0.3s ease both;
  }
  .hero-card {
    animation: fadeUp 0.6s 0.4s ease both;
  }
</style>
